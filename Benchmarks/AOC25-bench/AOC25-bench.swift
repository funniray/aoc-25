// Benchmark boilerplate generated by Benchmark

import Benchmark
import Foundation
import AOC25

nonisolated(unsafe) let benchmarks = {
    Benchmark("Day1-Parse") { benchmark in
        let _ = try! Day1(data: PackageResources.day_1_txt)
    }
    let day1 = try! Day1(data: PackageResources.day_1_txt)
    Benchmark("Day1-Part1") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day1.part1()
        }
    }
    Benchmark("Day1-Part2") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day1.part2()
        }
    }
    
    Benchmark("Day2-Parse") { benchmark in
        let _ = try! Day2(data: PackageResources.day_2_txt)
    }
    let day2 = try! Day2(data: PackageResources.day_2_txt)
    Benchmark("Day2-Part1") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day2.part1()
        }
    }
    Benchmark("Day2-Part2") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day2.part2()
        }
    }
    
    Benchmark("Day3-Parse") { benchmark in
        let _ = try! Day3(data: PackageResources.day_3_txt)
    }
    let day3 = try! Day3(data: PackageResources.day_3_txt)
    Benchmark("Day3-Part1") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day3.part1()
        }
    }
    Benchmark("Day3-Part2") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day3.part2()
        }
    }
    
    Benchmark("Day4-Parse") { benchmark in
        let _ = try! Day4(data: PackageResources.day_4_txt)
    }
    let day4 = try! Day4(data: PackageResources.day_4_txt)
    Benchmark("Day4-Part1") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day4.part1()
        }
    }
    Benchmark("Day4-Part2") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day4.part2()
        }
    }
    
    Benchmark("Day5-Parse") { benchmark in
        let _ = try! Day5(data: PackageResources.day_5_txt)
    }
    let day5 = try! Day5(data: PackageResources.day_5_txt)
    Benchmark("Day5-Part1") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day5.part1()
        }
    }
    Benchmark("Day5-Part2") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day5.part2()
        }
    }
    
    Benchmark("Day6-Parse") { benchmark in
        let _ = try! Day6(data: PackageResources.day_6_txt)
    }
    let day6 = try! Day6(data: PackageResources.day_6_txt)
    Benchmark("Day6-Part1") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day6.part1()
        }
    }
    Benchmark("Day6-Part2") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day6.part2()
        }
    }
    
    Benchmark("Day7-Parse") { benchmark in
        let _ = try! Day7(data: PackageResources.day_7_txt)
    }
    let day7 = try! Day7(data: PackageResources.day_7_txt)
    Benchmark("Day7-Solve") { benchmark in
        for _ in benchmark.scaledIterations {
            let _ = day7.solve()
        }
    }
    // Add additional benchmarks here
}
